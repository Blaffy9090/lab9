@model KategoriyaTovara

@{
    ViewData["Title"] = "Details";
}

<h1>Просмотр категории</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.NazvanieKategorii)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.NazvanieKategorii)
        </dd>
        <dt class="col-sm-2">
            Количество товаров
        </dt>
        <dd class="col-sm-10">
            @Model.Tovary?.Count()
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.KategoriyaTovaraId">Редактировать</a> |
    <a asp-action="Index">Возврат к списку категорий</a>
</div>